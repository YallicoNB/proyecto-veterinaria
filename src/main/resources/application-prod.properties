# Perfil prod — Render Web Service + Neon PostgreSQL

# Render asigna el puerto dinamicamente via $PORT
server.port=${PORT:8080}

# Neon PostgreSQL (sobrescribe la config de MySQL del application.properties)
spring.datasource.url=jdbc:postgresql://ep-young-resonance-aji5tli7.c-3.us-east-2.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_g30CNYUMsutl
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
